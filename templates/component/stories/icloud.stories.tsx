import {ComponentName} from '../src';

export default {
    title: '{ComponentName}',
    component: {ComponentName},
};

export const Demo = () => {
    return (
        <{ComponentName} />
    );
};
