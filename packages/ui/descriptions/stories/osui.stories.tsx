import React from 'react';
import Descriptions from '../src';

export default {
    title: 'Descriptions',
    component: Descriptions,
};

export const Demo = () => {
    return (
        <Descriptions />
    );
};
