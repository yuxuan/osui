import React from 'react';
import Descriptions from '../src';

export default {
    title: '未实现/Descriptions',
    component: Descriptions,
};

export const Demo = () => {
    return (
        <Descriptions />
    );
};
