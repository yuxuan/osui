import React from 'react';
import Skeleton from '../src';

export default {
    title: '未实现/Skeleton',
    component: Skeleton,
};

export const Demo = () => {
    return (
        <Skeleton />
    );
};
