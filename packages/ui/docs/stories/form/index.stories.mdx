import {Meta, Story, Canvas} from '@storybook/addon-docs';
import * as stories from './index.stories';

<Meta title="设计文档/Form 表单"  />

# OSUI Form

## API

### 基本属性

支持所有antd属性：见[antd文档](https://ant.design/components/form-cn/)

### OSUI新增属性

#### Form.Item

| 参数 | 说明 | 类型 | 默认值
| --- | --- | --- | --- |
| validateMessageLayout | 报错信息展示位置 | 'inline', 'default' | 'default' |

### 说明：
`validateMessageLayout`有两种`inline`|`default`，`default`垂直排放，`inline`会在Item后面显示，`inline`没有经过完善的实际使用测试，有可能有样式问题。

## 示例

** 注意: **  BrandProvider应该是App级别的，不是组件级别的。 BrandProvider的用法应该是：

```
// App入口
<BrandProvider brand="icloud">
  <App />
</BrandProvider>
```

### 基础表单

<Canvas>
  <Story name="StatusDemo" story={stories.BasicDemo} />
</Canvas>

### 用户名密码

<Canvas>
  <Story name="Demo" story={stories.Demo} />
</Canvas>

### 验证消息提示

<Canvas>
  <Story name="ValidateMessageDemo" story={stories.ValidateMessageDemo} />
</Canvas>
