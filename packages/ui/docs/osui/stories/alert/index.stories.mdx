import {Meta, Story, Preview} from '@storybook/addon-docs/blocks';
import * as stories from './index.stories';

<Meta title="设计文档/Alert 警告提示" component={Alert}/>

# OSUI Alert

## API

### 基本属性

支持所有antd属性：见[antd文档](https://ant.design/components/alert-cn/)

### 新增属性

| 参数 | 说明 | 类型 | 默认值
| --- | --- | --- | --- |
| actions | message中增加的action，例如：查看文档连接 | ReactNode | - |
| expandable | 是否展开收起展示message，为true时只展示一行内容 | boolean | - |
| countDown | 倒计时自动关闭alert | number | -1 |
### 其他说明

- 若icon不传并且存在description,则会赋予默认的icon， 不会通过`shouldStopClose`控制，点击则关闭
- 当`closeText`会裹一个span，通过`shouldStopClose`来控制，点击时是否关闭alert。

<Preview>
  <Story name="Demo" story={stories.Demo} />
</Preview>
