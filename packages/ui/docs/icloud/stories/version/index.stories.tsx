import React from 'react';
import version from '@osui/version';

export default {
    title: '通过验收/version',
};

export const Demo = () => {
    return (
        <h1>Antd Version: {version}</h1>
    );
};
