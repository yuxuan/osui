import React from 'react';
import version from '@osui/version';

export default {
    title: 'FE/version',
};

export const Demo = () => {
    return (
        <h1>Antd Version: {version}</h1>
    );
};
