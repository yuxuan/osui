import React from 'react';
import Transfer from '@osui/transfer';

export default {
    title: '未实现/Transfer',
    component: Transfer,
};

export const Demo = () => {
    return (
        <Transfer />
    );
};
