import React from 'react';
import Result from '@osui/result';

export default {
    title: '未实现/Result',
    component: Result,
};

export const Demo = () => {
    return (
        <Result />
    );
};
