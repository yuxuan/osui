import React from 'react';
import Comment from '@osui/comment';

export default {
    title: '未实现/Comment',
    component: Comment,
};

export const Demo = () => {
    return (
        <Comment />
    );
};
