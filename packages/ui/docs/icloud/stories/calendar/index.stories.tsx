import React from 'react';
import Calendar from '@osui/calendar';

export default {
    title: '未实现/Calendar',
    component: Calendar,
};

export const Demo = () => {
    return (
        <Calendar />
    );
};
