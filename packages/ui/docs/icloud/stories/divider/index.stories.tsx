import React from 'react';
import Divider from '@osui/divider';

export default {
    title: '未实现/Divider',
    component: Divider,
};

export const Demo = () => {
    return (
        <Divider />
    );
};
