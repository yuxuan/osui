import React from 'react';
import Layout from '@osui/layout';

export default {
    title: '未实现/Layout',
    component: Layout,
};

export const Demo = () => {
    return (
        <Layout />
    );
};
