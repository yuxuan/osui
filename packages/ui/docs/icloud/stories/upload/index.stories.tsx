import React from 'react';
import Upload from '@osui/upload';

export default {
    title: '未实现/Upload',
    component: Upload,
};

export const Demo = () => {
    return (
        <Upload />
    );
};
