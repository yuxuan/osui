import React from 'react';
import Transfer from '../src';

export default {
    title: 'Transfer',
    component: Transfer,
};

export const Demo = () => {
    return (
        <Transfer />
    );
};
