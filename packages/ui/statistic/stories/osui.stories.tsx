import React from 'react';
import Statistic from '../src';

export default {
    title: 'Statistic',
    component: Statistic,
};

export const Demo = () => {
    return (
        <Statistic />
    );
};
