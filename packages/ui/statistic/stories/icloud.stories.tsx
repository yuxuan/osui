import React from 'react';
import Statistic from '../src';

export default {
    title: '未实现/Statistic',
    component: Statistic,
};

export const Demo = () => {
    return (
        <Statistic />
    );
};
