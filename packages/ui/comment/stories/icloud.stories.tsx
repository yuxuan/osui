import React from 'react';
import Comment from '../src';

export default {
    title: '未实现/Comment',
    component: Comment,
};

export const Demo = () => {
    return (
        <Comment />
    );
};
