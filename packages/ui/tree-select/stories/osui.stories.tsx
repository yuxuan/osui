import React from 'react';
import {TreeSelect} from 'antd';

export default {
    title: 'TreeSelect',
    component: TreeSelect,
};

export const Demo = () => {
    return (
        <TreeSelect />
    );
};
