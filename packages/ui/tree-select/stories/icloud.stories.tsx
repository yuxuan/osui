import React from 'react';
import {TreeSelect} from 'antd';

export default {
    title: '验收中/TreeSelect 树选择',
    component: TreeSelect,
};

export const Demo = () => {
    return (
        <TreeSelect />
    );
};
