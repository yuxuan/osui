import React from 'react';
import Layout from '../src';

export default {
    title: '未实现/Layout',
    component: Layout,
};

export const Demo = () => {
    return (
        <Layout />
    );
};
