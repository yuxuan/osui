import React from 'react';
import Carousel from '../src';

export default {
    title: 'Carousel',
    component: Carousel,
};

export const Demo = () => {
    return (
        <Carousel />
    );
};
