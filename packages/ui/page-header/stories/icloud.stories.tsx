import React from 'react';
import PageHeader from '../src';

export default {
    title: '未实现/PageHeader',
    component: PageHeader,
};

export const Demo = () => {
    return (
        <PageHeader />
    );
};
