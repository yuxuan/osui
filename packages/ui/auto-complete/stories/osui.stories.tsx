import React from 'react';
import {AutoComplete} from 'antd';

export default {
    title: 'AutoComplete',
    component: AutoComplete,
};

export const Demo = () => {
    return (
        <AutoComplete />
    );
};
