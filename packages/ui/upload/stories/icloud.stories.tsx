import React from 'react';
import Upload from '../src';

export default {
    title: '未实现/Upload',
    component: Upload,
};

export const Demo = () => {
    return (
        <Upload />
    );
};
