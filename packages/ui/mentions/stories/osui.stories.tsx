import React from 'react';
import Mentions from '../src';

export default {
    title: 'Mentions',
    component: Mentions,
};

export const Demo = () => {
    return (
        <Mentions />
    );
};
