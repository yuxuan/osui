import React from 'react';
import Mentions from '../src';

export default {
    title: '未实现/Mentions',
    component: Mentions,
};

export const Demo = () => {
    return (
        <Mentions />
    );
};
