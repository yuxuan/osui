import React from 'react';
import Divider from '../src';

export default {
    title: '未实现/Divider',
    component: Divider,
};

export const Demo = () => {
    return (
        <Divider />
    );
};
