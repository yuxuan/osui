import React from 'react';
import Calendar from '../src';

export default {
    title: 'Calendar',
    component: Calendar,
};

export const Demo = () => {
    return (
        <Calendar />
    );
};
