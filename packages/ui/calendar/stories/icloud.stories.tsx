import React from 'react';
import Calendar from '../src';

export default {
    title: '未实现/Calendar',
    component: Calendar,
};

export const Demo = () => {
    return (
        <Calendar />
    );
};
